// Write a function that receives the dimension of a picture and returns true if its in landscape mode
const isLandscape = (height, width) => width > height;

// testing
console.log(isLandscape(10, 50));
