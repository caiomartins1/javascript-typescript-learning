/**
 * Some flags while installing packages:
 *  -> -E (exact current version)
 *  -> -D (dev dependencies)
 *  -> packagename@version (install the version specificated)
 *  -> -depth=0 (depth of the listing)
 *
 * Some commands:
 *  -> npm install
 *  -> npm run script-name (package.json)
 *  -> npm update (updates all packages)
 *  -> npm uninstall
 *  -> npm ls (list all packages and dependencies)
 *  -> npm outdated
 */
